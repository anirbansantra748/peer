# DATABASE & CACHING
MONGO_URI=[REDACTED]
# MONGO_URI=mongodb://localhost:27017/peer
# REDIS_URL=[REDACTED]
REDIS_URL=redis://localhost:6379

# APP CONFIG
API_PORT=3001
UI_PORT=3000
BASE_URL=http://localhost:3000

# GITHUB INTEGRATION
GITHUB_APP_ID=[REDACTED]
GITHUB_TOKEN=[REDACTED]
GITHUB_WEBHOOK_SECRET=[REDACTED]
GITHUB_APP_WEBHOOK_SECRET=[REDACTED]

# LLM PROVIDERS (FREE TIER)
# Groq (Primary for simple/fast fixes) - FREE 14,400 req/day
GROQ_API_KEY=[REDACTED]
GROQ_MODEL=llama-3.3-70b-versatile

# DeepSeek (Out of credits - disabled for now)
DEEPSEEK_API_KEY=[REDACTED]
DEEPSEEK_MODEL=deepseek-coder

# Gemini (Fallback for both) - FREE 1,500 req/day
GEMINI_API_KEY=[REDACTED]
GEMINI_MODEL=gemini-2.5-flash
GEMINI_API_BASE=https://generativelanguage.googleapis.com/v1beta/models

# OpenAI (Emergency fallback only)
OPENAI_API_KEY=[REDACTED]
OPENAI_MODEL=gpt-4o-mini

# LLM CONFIGURATION
# LLM_PROVIDER=auto
LLM_REWRITE_MODE=always
LLM_STRATEGY=full
LLM_TIMEOUT_MS=30000
LLM_CONCURRENCY=3
LLM_MAX_PATCHES_PER_FILE=5
LLM_MULTILINE_ALLOWED=0
LLM_DEBUG=1

# PREVIEW / AI FIXING
PREVIEW_INITIAL_MAX_FILES=30
PREVIEW_TIME_BUDGET_MS=30000
MIN_AI_SEVERITY=high


# In your .env
REDIS_CACHE_ENABLED=true
REDIS_CACHE_TTL=86400  # 24 hours

OPENROUTER_API_KEY=[REDACTED]
OPENROUTER_MODEL=mistralai/mistral-7b-instruct

# Force OpenRouter usage:
LLM_PROVIDER=openrouter

# GitHub OAuth
GITHUB_CLIENT_ID=[REDACTED]
GITHUB_CLIENT_SECRET=[REDACTED]
GITHUB_CALLBACK_URL=http://localhost:3000/auth/github/callback

# Session & Security
SESSION_SECRET=[REDACTED]
ENCRYPTION_KEY=[REDACTED]
NODE_ENV=development

GITHUB_APP_PRIVATE_KEY="[REDACTED]"

# smtp
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=anirbansantra748@gmail.com
SMTP_PASS=[REDACTED]
SMTP_FROM=anirbansantra748@gmail.com

# Razorpay Configuration
RAZORPAY_KEY_ID=[REDACTED]
RAZORPAY_KEY_SECRET=[REDACTED]
RAZORPAY_WEBHOOK_SECRET=[REDACTED]

# Pricing (you can change these)
RAZORPAY_PRO_PRICE=800
# RAZORPAY_ENTERPRISE_PRICE=5000
ADMIN_EMAIL=anirbansantra748@gmail.com
