<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title><%= title %></title>
    <style>
      body { font-family: system-ui, Arial, sans-serif; padding: 20px; }
      .status { padding: 8px; border-radius: 4px; background: #eef; display: inline-block; }
      .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
      pre { background: #f6f8fa; padding: 12px; border: 1px solid #ddd; overflow: auto; }
      .ok { color: #0a8; }
      .warn { color: #d77a00; }
    </style>
  </head>
  <body>
    <h1>Patch Results — Run <%= runId %></h1>
    <p>Patch Request: <code><%= patchRequestId %></code></p>
    <p>Status: <span class="status"><%= patch.status %></span></p>

    <% if (patch.results) { %>
      <h3>Git</h3>
      <ul>
        <li>Branch: <code><%= patch.results.branchName || '(n/a)' %></code></li>
        <li>Commit: <code><%= patch.results.commitSha || '(n/a)' %></code></li>
      </ul>
      <div class="grid">
        <div>
          <h3>Applied (<%= (patch.results.applied || []).length %>)</h3>
          <ul>
            <% (patch.results.applied || []).forEach(a => { %>
              <li class="ok"><code><%= a.file %></code> — finding <code><%= a.findingId %></code></li>
            <% }) %>
          </ul>
        </div>
        <div>
          <h3>Skipped (<%= (patch.results.skipped || []).length %>)</h3>
          <ul>
            <% (patch.results.skipped || []).forEach(s => { %>
              <li class="warn"><code><%= s.file %></code> — finding <code><%= s.findingId %></code> — <%= s.reason %></li>
            <% }) %>
          </ul>
        </div>
      </div>
      <% if ((patch.results.errors || []).length) { %>
        <h3>Errors</h3>
        <ul>
          <% (patch.results.errors || []).forEach(err => { %>
            <li class="warn"><%= err.message %></li>
          <% }) %>
        </ul>
      <% } %>
    <% } %>

    <% if (patch.status !== 'completed' && patch.status !== 'failed') { %>
      <p>Patch is still processing. <a href="/runs/<%= runId %>/patches/<%= patchRequestId %>">Refresh</a></p>
    <% } %>

    <div style="margin: 20px 0; padding: 16px; background: #f8f9fa; border-radius: 6px; border-left: 4px solid #0066cc;">
      <h3 style="margin-top: 0;">Re-run AI Fixes</h3>
      <p style="color: #666; margin: 8px 0;">Generate new fixes for the same issues using the latest AI model. This will create a new preview with potentially improved suggestions.</p>
      <form method="post" action="/runs/<%= runId %>/rerun" style="display: inline;">
        <input type="hidden" name="patchRequestId" value="<%= patchRequestId %>">
        <button type="submit" style="padding: 8px 16px; background: #0066cc; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 14px; font-weight: 500;">🔄 Re-run AI Fixes</button>
      </form>
    </div>

    <p><a href="/runs/<%= runId %>/select">Back to selection</a></p>
  </body>
</html>
