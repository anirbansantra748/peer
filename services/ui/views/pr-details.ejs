<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        background: #0d1117;
        color: #c9d1d9;
        padding: 20px;
        line-height: 1.6;
      }
      .container { max-width: 1400px; margin: 0 auto; }
      
      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
        padding-bottom: 20px;
        border-bottom: 1px solid #21262d;
      }
      h1 { 
        font-size: 28px;
        font-weight: 600;
        color: #58a6ff;
      }
      .nav-links {
        display: flex;
        gap: 15px;
      }
      .nav-links a {
        color: #8b949e;
        text-decoration: none;
        font-size: 14px;
      }
      .nav-links a:hover { color: #58a6ff; }
      
      .pr-header {
        background: #161b22;
        border: 1px solid #30363d;
        border-radius: 8px;
        padding: 24px;
        margin-bottom: 20px;
      }
      .pr-title {
        font-size: 24px;
        font-weight: 600;
        color: #c9d1d9;
        margin-bottom: 12px;
      }
      .pr-meta {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 16px;
        margin-top: 16px;
      }
      .meta-item {
        display: flex;
        flex-direction: column;
        gap: 4px;
      }
      .meta-label {
        font-size: 11px;
        color: #8b949e;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }
      .meta-value {
        font-size: 16px;
        font-weight: 600;
        color: #c9d1d9;
      }
      
      .section {
        background: #161b22;
        border: 1px solid #30363d;
        border-radius: 8px;
        padding: 24px;
        margin-bottom: 20px;
      }
      .section-title {
        font-size: 18px;
        font-weight: 600;
        color: #c9d1d9;
        margin-bottom: 20px;
        display: flex;
        align-items: center;
        gap: 8px;
      }
      
      .issue-card {
        background: #0d1117;
        border: 1px solid #30363d;
        border-radius: 6px;
        padding: 16px;
        margin-bottom: 12px;
        transition: border-color 0.2s;
      }
      .issue-card:hover { border-color: #58a6ff; }
      .issue-card.fixed { border-left: 4px solid #3fb950; }
      .issue-card.pending { border-left: 4px solid #f0883e; }
      
      .issue-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 8px;
      }
      .issue-title {
        font-size: 14px;
        font-weight: 500;
        color: #c9d1d9;
      }
      .issue-badge {
        padding: 4px 8px;
        border-radius: 12px;
        font-size: 11px;
        font-weight: 600;
      }
      .badge-critical { background: #a40e26; color: #ffdce0; }
      .badge-high { background: #9a6700; color: #f0e6b9; }
      .badge-medium { background: #0969da; color: #a5d6ff; }
      .badge-low { background: #6e7681; color: #c9d1d9; }
      
      .issue-details {
        font-size: 13px;
        color: #8b949e;
        margin-top: 8px;
      }
      .issue-meta {
        display: flex;
        gap: 16px;
        font-size: 12px;
        color: #6e7681;
        margin-top: 12px;
      }
      
      .fix-status {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 6px 12px;
        border-radius: 6px;
        font-size: 13px;
        font-weight: 500;
      }
      .fix-status.fixed { background: #1a7f37; color: #aff5b4; }
      .fix-status.pending { background: #9a6700; color: #f0e6b9; }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <h1>📦 PR Details</h1>
        <div class="nav-links">
          <a href="/">← Dashboard</a>
          <a href="/audits">Audit Logs</a>
        </div>
      </header>
      
      <div class="pr-header">
        <div class="pr-title">
          <%= run.repo %> #<%= run.prNumber %>
        </div>
        <div style="font-size: 13px; color: #8b949e; margin-bottom: 16px;">
          Commit: <code style="color: #58a6ff;"><%= run.sha ? run.sha.substring(0, 7) : 'N/A' %></code>
        </div>
        
        <div class="pr-meta">
          <div class="meta-item">
            <div class="meta-label">Status</div>
            <div class="meta-value" style="color: <%= run.status === 'completed' ? '#3fb950' : run.status === 'failed' ? '#da3633' : '#58a6ff' %>">
              <%= run.status.toUpperCase() %>
            </div>
          </div>
          <div class="meta-item">
            <div class="meta-label">Total Issues</div>
            <div class="meta-value" style="color: #f0883e;"><%= findings.length %></div>
          </div>
          <div class="meta-item">
            <div class="meta-label">Issues Fixed</div>
            <div class="meta-value" style="color: #3fb950;"><%= fixedCount %></div>
          </div>
          <div class="meta-item">
            <div class="meta-label">Fix Rate</div>
            <div class="meta-value"><%= fixRate %>%</div>
          </div>
          <div class="meta-item">
            <div class="meta-label">Created</div>
            <div class="meta-value" style="font-size: 13px;"><%= new Date(run.createdAt).toLocaleString() %></div>
          </div>
        </div>
      </div>
      
      <div class="section">
        <div class="section-title">
          🔍 Issues Found (<%= findings.length %>)
        </div>
        
        <% if (findings.length === 0) { %>
          <p style="color: #6e7681; text-align: center; padding: 40px 0;">No issues found in this PR.</p>
        <% } else { %>
          <% findings.forEach(finding => { %>
            <div class="issue-card <%= finding.fixed ? 'fixed' : 'pending' %>">
              <div class="issue-header">
                <div class="issue-title">
                  <%= finding.message %>
                </div>
                <span class="issue-badge badge-<%= finding.severity %>">
                  <%= finding.severity.toUpperCase() %>
                </span>
              </div>
              
              <div class="issue-details">
                <strong>File:</strong> <code><%= finding.file %></code> 
                · <strong>Line:</strong> <%= finding.line %>
                · <strong>Rule:</strong> <%= finding.rule %>
                · <strong>Analyzer:</strong> <%= finding.analyzer %>
              </div>
              
              <div class="issue-meta">
                <div>
                  <% if (finding.fixed) { %>
                    <span class="fix-status fixed">✅ Fixed</span>
                    <% if (finding.fixedAt) { %>
                      on <%= new Date(finding.fixedAt).toLocaleString() %>
                    <% } %>
                  <% } else { %>
                    <span class="fix-status pending">⏳ Pending</span>
                  <% } %>
                </div>
              </div>
            </div>
          <% }) %>
        <% } %>
      </div>
      
      <div style="margin-top: 24px;">
        <a href="/runs/<%= run._id %>/select" style="padding: 12px 20px; background: #238636; color: white; text-decoration: none; border-radius: 6px; display: inline-block; font-weight: 500;">
          🔧 Select Issues to Fix
        </a>
      </div>
    </div>
  </body>
</html>
